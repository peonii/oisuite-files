#include <bits/stdc++.h>

int32_t main(void) {
    std::cout << "Hello, world!";
    
    /*
     * Here you can write
     * your inefficient brute-force
     * algorithm! oisuite will automatically
     * generate testcases based on the restrictions provided
     * in generate_tests.cpp!
     */

    return 0;
}
