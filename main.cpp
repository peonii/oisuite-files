#include <bits/stdc++.h>

int32_t main(void) {
    ios_base::sync_with_stdio(false);
    std::cin.tie(NULL);
    std::cout << "Hello, world!";
    
    /*
     * Here you can write your
     * algorithm! Make sure to remove
     * the Hello, world! line!
     *
     * Make sure to test your algorithm using the
     * brute-force version using "oisuite test <name>"!
     */

    return 0;
}
